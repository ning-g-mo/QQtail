/**引入库 */
import qtail from 'https://cdn.jsdelivr.net/npm/qtail-js@latest/+esm';
/**初始化 */
const a = new qtail("","");
/**获取值 */
document.getElementById('qz').value
document.getElementById('wb').value
a.qtail.update("qz");
a.qtail.upd("wb");

/**输出值 */

qtail.generate(a); {
     sc.value = a.qtail.get()
}
qtail.gen(a)